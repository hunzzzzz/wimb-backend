<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moira.wimb.domain.infra.mapper.ApiResultMapper">
    <select id="selectRepeatInOneMinuteChk" resultType="java.lang.Boolean">
        SELECT EXISTS (
            SELECT 1
            FROM wimb.api_result
            WHERE email = #{email}
             AND  status != 'FAIL'
             AND  requested_at <![CDATA[ >= ]]> CURRENT_TIMESTAMP - INTERVAL '1 minute'
             AND  requested_at <![CDATA[ <= ]]> CURRENT_TIMESTAMP
        )
    </select>

    <insert id="insertApiResult" useGeneratedKeys="true" keyProperty="seqNo" keyColumn="seq_no">
        INSERT INTO wimb.api_result (
               type
             , email
             , s3_key
             , status
             , fail_message
             , requested_at
             , responded_at
        )
        VALUES (
               #{type}
             , #{email}
             , #{s3Key}
             , #{status}
             , #{failMessage}
             , #{requestedAt}
             , #{respondedAt}
        )
    </insert>

    <update id="updateStatus">
        UPDATE wimb.api_result
        SET   status = #{status}
        WHERE seq_no = #{seqNo}
    </update>

    <update id="updateStatusAndFailMessage">
        UPDATE wimb.api_result
        SET    status       = #{status}
             , fail_message = #{failMessage}
        WHERE  seq_no       = #{seqNo}
    </update>
</mapper>